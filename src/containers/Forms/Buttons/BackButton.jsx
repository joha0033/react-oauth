import React from 'react'



const BackButton = ({text}) => (
      <div>
        <br/>
        <input
          type="button"
          value={text}
          />
        <br/>
      </div>
)

export default BackButton