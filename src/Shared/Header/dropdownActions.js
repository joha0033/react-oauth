// importing actions from UserCredentials / Signin / Signin Actions
import { userActions } from "../../UserCredentials/Signin/userActions"

// const login = userActions.login

const logout = userActions.logout

export const dropdownActions = {
    // login,
    logout
}